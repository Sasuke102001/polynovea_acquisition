# Install: pip install -r requirements.txt --only-binary=:all:
# Run:     uvicorn main:app --reload --port 8000
#
# asyncpg requires a pre-built binary wheel on Windows Python 3.14:
#   pip install asyncpg --only-binary=:all:

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
asyncpg>=0.29.0
pydantic>=2.11.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.1
httpx>=0.27.0
openai>=1.12.0
