# Copy to .env and fill in — or just use the project root .env (loaded automatically)
NVIDIA_API_KEY=nvapi-...

# Optional: override the extraction model (default: meta/llama-3.3-70b-instruct)
# NVIDIA_MODEL_EXTRACTION=meta/llama-3.3-70b-instruct
